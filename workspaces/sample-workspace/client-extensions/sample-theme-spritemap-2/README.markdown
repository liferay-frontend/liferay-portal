# sample-theme-spritemap-2

- This sample contains a build step that generates the spritemap from a collection of icons found in `./src`.
- This sample does not extend the current clay spritemap
- This sample only contains one icon, `cog`