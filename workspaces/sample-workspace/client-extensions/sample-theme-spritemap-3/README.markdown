# sample-theme-spritemap-3

- This sample contains a build step that generates the spritemap from a collection of icons found in `./src`.
- This sample extends the current clay spritemap and new icons will override existing ones.
- The `cog` icon will be replaced by an icon that looks like books.
- The `feather` icon is a new icon that is added to the spritemap